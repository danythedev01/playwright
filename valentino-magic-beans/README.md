No need to run a js server since we're using the codegen

```bash
npx playwright codegen https://valentinos-magic-beans.click
```

Running specific tests with ui:

```bash
npx playwright test tests/basic --ui
```
